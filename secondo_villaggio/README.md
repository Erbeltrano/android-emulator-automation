# Bot Villaggio Costruttori (secondo villaggio) — stato del lavoro

Bot per il **Villaggio Costruttori** di Clash of Clans. Codice vero in `bot_costruttori.py` (v0.30 al 2026-08-14), standalone rispetto al bot del villaggio primario (`BOT_COMPLETO_MAC.py`): i due non possono girare insieme, perché pilotano la stessa istanza BlueStacks via ADB. Leggi questo file insieme al `README.md` alla radice (architettura remota condivisa: mini PC, PC Windows, ADB, Telegram) e a `recap.md` (cronologia completa, molto più dettagliata, di come si è arrivati qui — in particolare le sessioni del 2026-07-30/31 e del 2026-08-04).

**v0.30 (2026-08-14, bug vero)**: l'utente ha segnalato che il bot, avviato da Telegram, si chiudeva pochi secondi dopo l'avvio. Causa: il blocco che aggiunge il timestamp a `print()` (introdotto in v0.29) era duplicato per errore nel file — alla seconda esecuzione `_builtin_print = print` catturava già il wrapper della prima invece del `print` originale di Python, quindi la nuova `print()` richiamava se stessa all'infinito (`RecursionError: maximum recursion depth exceeded` sulla primissima riga di log). Fix: rimosso il blocco duplicato — resta una sola ridefinizione, identica a quella del bot principale. **Confermato dal vivo**: riavviato il task Windows `CoCBotCostruttori`, il bot resta in esecuzione ed esegue un ciclo di attacco completo senza errori.

**v0.29 (2026-08-12)**: nessun bug, solo ritocchi richiesti dall'utente + robustezza portata dal villaggio primario nella stessa sessione. `WALL_UPGRADE_EVERY` abbassato da 20 a 14 cicli e `COLLECT_ELIXIR_EVERY` dimezzato da 10 a 5 (entrambi su richiesta esplicita). Portata anche `_adb_recover()` (nuova nel villaggio primario v3.15, stesso giorno): se i retry ravvicinati di `_adb_retry`/`adb_screenshot` falliscono tutti, riavvia il server adb e ripesca il device prima di arrendersi — qui non si era mai osservato il blocco del bridge ADB che ha colpito il villaggio primario, ma è lo stesso identico bridge, quindi applicato per prevenzione. Aggiunto anche il timestamp a tutte le righe di `bot_costruttori_log.txt` (prima assente) e lo stderr reale negli `[ERRORE]` di log.

**v0.27-v0.28 (2026-08-11, stessa sessione, dopo il v0.26)**: anche col dezoom del v0.26, l'utente guardando dal vivo ha continuato a vedere la PRIMA truppa (la prima strega, subito dopo l'eroe nella barra) a volte non selezionarsi affatto sul secondo raid - lo schieramento sembrava partire dalla seconda. v0.27 (tentativo insufficiente): ipotesi che il tap sull'eroe (primo della sequenza) "mangiasse" quello immediatamente successivo sulla prima truppa - aggiunta una pausa extra (`HERO_TO_FIRST_TROOP_EXTRA_DELAY=0.6s`) solo tra questi due tap. L'utente ha confermato dal vivo che il problema persisteva ancora **in modo incostante** ("a volte lo fa e a volte no") - segno di un tap ADB perso in modo probabilistico, non di un ritardo fisso da allungare ulteriormente. **v0.28 (fix confermato funzionante)**: invece di altre pause, la coppia di tap (selezione sulla barra + sgancio sulla mappa) per la sola prima truppa viene ripetuta due volte - sicuro anche se il primo giro è già andato a segno (uno slot già usato non fa nulla se ritoccato). **Confermato dal vivo dall'utente** dopo il deploy: "si confermo".

**v0.26 (2026-08-11, stessa sessione, causa vera trovata dall'utente)**: dopo il v0.23/v0.25 restava un problema - guardando il bot dal vivo, l'utente ha notato che sul secondo raid una truppa (non sempre la stessa) veniva selezionata nella barra ma non piazzata sulla mappa. L'utente stesso ha identificato la causa: **la camera perde lo zoom minimo specificamente all'inizio del secondo raid** (un nuovo matchmaking sembra resettarlo), non solo al boot di BlueStacks (dove viene già corretto una volta da `windows/dezoom_camera.ps1` a inizio sessione). Con uno zoom diverso da quello calibrato, `DEPLOY_POINTS` non cade più nella zona valida per alcune unità. Fix: nuova `_dezoom_camera()` che richiama lo stesso script di dezoom dell'avvio, ma a runtime dal processo Python stesso (funziona per lo stesso motivo per cui funziona all'avvio: il processo gira dentro la sessione desktop interattiva vera, task Windows con logon interattivo) - chiamata subito prima di schierare sul secondo raid. **Confermato dal vivo su 2 raid consecutivi dopo il deploy**: tutte le 8 unità piazzate correttamente su entrambi (160% e 130% danno), incluse quelle che prima restavano bloccate. Pulizia minore nello stesso giro: `dezoom_camera.ps1` perdeva un `True` non catturato nello stdout (leggeva `SetCursorPos` senza `| Out-Null`) - finiva nei log del bot ad ogni chiamata, corretto.

**v0.24-v0.25 (stessa sessione, tentativi intermedi)**: prima di trovare la causa vera (sopra), provate una breve pausa fissa prima di schierare (`RAID2_DEPLOY_SETTLE=1.2s`, v0.24) e un ritmo di tap leggermente più lento nel loop di schieramento (v0.25) - entrambi ragionevoli ma non la causa reale, lasciati comunque attivi (non fanno male, danno un minimo di margine in più).

**v0.23 (2026-08-11, stessa sessione)**: l'utente, guardando il bot dal vivo, ha segnalato che l'attesa attiva introdotta in v0.20 prima di schierare sul secondo raid (aspettare che il banner passasse da "inizia" a "termina") faceva perdere troppo tempo ad ogni ciclo, e che il comportamento precedente (schierare SUBITO appena letto "la battaglia inizia tra") funzionava bene. Attesa rimossa: si schiera non appena rilevato l'"inizia", stesso istante di `wait_for_next_screen()`. **Confermato dal vivo subito dopo**: screenshot catturato a pochi secondi dal deploy mostra la vera schermata di battaglia ("la battaglia termina tra: 1m48s", bottone "Resa"/"Termina battaglia", streghe ed eroe visibilmente schierati e attivi, già 119% danno/3 stelle) - non la schermata di anteprima temuta dal v0.20. Ciclo completato pulito, nessun errore. Se il vecchio bug del v0.19 (truppe sprecate sul secondo raid) dovesse ripresentarsi, la diagnosi del v0.20 (differenza visiva tra le due schermate) resta comunque documentata nel codice come primo indizio da ricontrollare.

**v0.22 (2026-08-11)**: bug reale segnalato dall'utente — il popup "Bonus stella ottenuto" bloccava il bot senza che nessuno lo chiudesse, e il ciclo finiva per fallire con l'errore "popup imprevisto bloccato?" (visto dal vivo alle 09:05 di quel giorno). Causa: dalla v0.18 il tap sul bottone OK di quel popup era stato rimosso del tutto (era una coordinata sulla MAPPA, non un elemento UI fisso — rischiava di aprire un edificio vero se il popup non c'era, vedi sotto), e la rete di sicurezza `_ensure_home_or_recover()` tocca solo una zona vuota della mappa, che non chiude un vero dialog modale (serve il suo bottone OK). Fix: nuova `_dismiss_star_bonus_popup()` rileva il popup per CONTENUTO reale via OCR (testo "stella" in una regione ampia e centrale, `POPUP_SCAN_REGION`) — un segnale positivo, non una coordinata alla cieca come nel vecchio bug del v0.17/v0.18 — poi cerca il suo bottone "OK" nella stessa regione, anch'esso via OCR (non un offset fisso). Tocca solo se ENTRAMBI vengono trovati: innocuo se il popup non c'è. Richiamata da `_ensure_home_or_recover()` prima del tap generico sulla mappa. **Testato dal vivo**: sessione reale avviata dopo il deploy, 13 cicli completati (il primo con l'errore preesistente prima ancora che il fix potesse intervenire — struttura del ciclo già in corso — i successivi 12 tutti puliti, nessun altro errore/recovery necessario).

**v0.21 (2026-08-10)**: bug reale trovato dal vivo — l'utente ha segnalato che il bot "non partiva" quando avviato da fuori casa. `switch_to_village("costruttore")` falliva con entrambi i candidati pan+punto esistenti (la barca si trovava quel giorno in una posizione leggermente diversa, stesso tema ricorrente dello zoom/pan non deterministico tra un riavvio e l'altro). Fix: prima del tap puntuale cieco, si cerca la barca via `cv2.matchTemplate` (immagini di riferimento `boat_to_builder_ref.png`/`boat_to_primary_ref.png`) in una finestra centrata sul punto calibrato — tollera piccoli spostamenti (e, provando piu' scale, un po' di variazione di zoom) senza dover indovinare altri punti fissi; se non trova nulla ricade sul vecchio tap puntuale (nessuna regressione). **Confermato funzionante dal vivo con la funzione reale** in entrambe le direzioni. Aggiunto anche il rilevamento (senza tap automatico, vedi sotto) del dialog nativo "connessione interrotta per inattività" prima di procedere con "Cerca!"/`deploy_wave()`.

## Stato: automatizzato e collegato a Telegram

- **Flusso di attacco** (doppio raid + schieramento): scritto, testato dal vivo più volte — fino a **200% di danno totale, 3 stelle su entrambi i raid**.
- **Cambio villaggio automatico**: il bot controlla da solo all'avvio se il gioco è rimasto aperto sul villaggio sbagliato (primario invece di Costruttori) e passa da solo toccando la "barca", prima di iniziare. Ricalibrato il 2026-08-04 (v0.10) dopo che un nuovo badge dell'evento stagionale aveva iniziato a coprire il punto di tap originale.
  - **ATTENZIONE (v0.15/v0.16, 2026-08-09)**: il tap sulla barca resta impreciso - lo zoom/pan della camera dopo un riavvio a freddo non è deterministico (scoperto dal vivo, causa esatta non ancora capita), quindi `switch_to_village()` può ancora fallire nonostante `BOAT_TO_BUILDER/PRIMARY_CANDIDATES` provi più coordinate. **Il bot ora si ferma con un avviso Telegram invece di attaccare alla cieca sul villaggio sbagliato** se il cambio fallisce (era il bug reale trovato: prima procedeva comunque, aprendo per sbaglio popup di gioco invece di combattere - vedi `recap.md` per i dettagli).
  - **Causa radice vera del popup a fine ciclo trovata e fixata (v0.17→v0.18, stesso giorno, sessione serale)**: NON era il badge "Schede degli incarichi" - era `STAR_BONUS_OK_BUTTON` (960, 838), tappato "a vuoto se non serve" per chiudere un eventuale popup "Bonus stella!", che essendo una coordinata sulla MAPPA (non un elemento UI fisso) poteva aprire il pannello di un edificio vero (osservato: un muro) se la camera lo metteva lì sotto - il secondo tap, sullo stesso punto ora sul pannello aperto, è arrivato sul pulsante "Migliora" (oro), aprendo un vero dialog di conferma spesa da 640.000 oro (nessuna spesa avvenuta, ma a un tap di distanza). **Fix**: il tap è stato rimosso del tutto (non reso "più sicuro" con un controllo condizionale - un primo tentativo così, v0.17, è fallito perché anche `is_home_screen()` può sbagliarsi). Resta solo `_ensure_home_or_recover()` con un punto di recovery scelto apposta fuori dall'area costruibile.
  - **`is_home_screen()` ricalibrato (v0.18)**: il vecchio controllo a pixel singolo poteva cadere su un contorno nero del testo "Attacco!" invece che sul riempimento arancione del pulsante, dando falsi negativi. Sostituito con una regione (media su una striscia sotto il testo), verificata su più screenshot reali con margine ampio. **Confermato funzionante dal vivo con un ciclo completo end-to-end** dopo entrambi i fix.
  - **Non usare il tasto Indietro Android per la recovery**: testato dal vivo (v0.16), dopo un paio di pressioni sulla home screen fa comparire il dialog "Vuoi uscire dal gioco?" - rischio reale di chiudere BlueStacks per sbaglio. Si usa invece un tap su una zona vuota fuori mappa.
  - **Lezione generale**: qualunque tap/controllo a coordinate o pixel fissi in questo bot può essere impreciso a seconda dello zoom/pan con cui il gioco riparte (tema ricorrente di tutta la giornata dell'8/9 agosto, non solo la barca). La strategia che ha funzionato meglio è stata eliminare i tap non necessari invece di proteggerli con altri controlli a coordinate fisse. Una soluzione strutturale (ricerca di barca/pulsanti/popup via analisi immagine) resta da fare in una sessione dedicata.
- **Rilevamento fine-raid via OCR**: legge la scritta in alto ("La battaglia inizia/termina tra:") per sapere subito quando il raid corrente è finito e il successivo è pronto, invece di aspettare un tempo fisso.
  - **ATTENZIONE, bug vero fixato (v0.19→v0.20, 2026-08-09)**: "la battaglia INIZIA tra: Ns" (segnale "il secondo raid esiste") NON significa che si può schierare subito, come si pensava dalla v0.6 - è ancora un'ANTEPRIMA/scouting dell'avversario (bottone "Termina battaglia", camera larga, nessuna truppa), non la vera battaglia (quella ha "Resa"/"Termina battaglia" a seconda dei punti già fatti, camera vicina al bordo villaggio, "la battaglia TERMINA tra"). Il countdown "inizia" osservato dal vivo può durare fino a un minuto: un primo tentativo di fix (attesa fissa di 1.5s, v0.19) ha sprecato un raid intero (quasi zero truppe schierate, notato in tempo reale dall'utente). Fix vero: si aspetta attivamente (stesso meccanismo OCR, `RAID2_PREVIEW_MAX_WAIT=70s`) che il banner diventi "termina" prima di schierare sul secondo raid. **Confermato dal vivo su 2 secondi raid consecutivi**: uno atteso realmente 60s prima di schierare, 100% danno/3 stelle raggiunti, prima strega (quella che falliva sempre) correttamente schierata.
  - **ATTENZIONE, secondo problema trovato SUBITO DOPO v0.20, NON ANCORA RISOLTO**: monitorando dal vivo 6 schieramenti dopo il fix (5 ottimi: 90/97/100/104/120% danno, eroe+streghe chiaramente schierati), **1 caso scarso**: 28% danno, 0 stelle, eroe e streghe rimasti "colorati/disponibili" in barra invece che "grigi/usati". Timing normale stavolta (non lo stesso bug di sopra) - ipotesi da verificare: `DEPLOY_POINTS` (calibrati su UNA base di riferimento) potrebbero cadere in zona non valida su alcuni layout di base avversaria diversi. Serve raccogliere altri casi prima di intervenire; se confermato, il fix è il metodo della griglia (richiede l'utente presente). **Riprendere da qui alla prossima sessione** - vedi `recap.md`, sezione "quarto seguito, fine sessione".
- **Raccolta elisir dal carretto**: automatizzata, ogni 5 cicli (`COLLECT_ELIXIR_EVERY`, dimezzato dal v0.29 - era 10, prima ancora era ad ogni ciclo - cambiato il 2026-08-12 su richiesta esplicita dell'utente).
- **Avviabile da Telegram**: bottone "🏗️ Villaggio Secondario" → "▶️ Avvia" (vedi `minipc/telegram_relay.py` + task Windows `CoCBotCostruttori` + `windows/run_bot_costruttori.bat`).
- **Upgrade mura del Villaggio Costruttori**: `try_wall_upgrade()` scritta, **confermata funzionante con spesa reale verificata** e collegata al loop automatico ogni 20 cicli (v0.13, 2026-08-04) — vedi sezione dedicata sotto.

**Le coordinate esatte, le soglie OCR e la cronologia di calibrazione/debug sono nel codice stesso** (`bot_costruttori.py`, commenti estesi su ogni costante) e in `recap.md` — questo file dà solo il quadro d'insieme, per evitare di dover tenere sincronizzati due posti diversi con gli stessi numeri ogni volta che una coordinata viene ricalibrata (successo più volte nella sessione del 2026-07-30/31: swipe camera, coordinate del carretto, ecc.).

## Come funziona questo evento/villaggio

A differenza del villaggio principale:
- **Nessuno scouting**: appena premi "Cerca!" trovi subito un avversario, nessuna schermata di bottino da valutare prima.
- **Doppio attacco**: ogni "giro" sono in realtà **due raid consecutivi**. Il secondo si sblocca solo dopo aver distrutto il primo al 100% — **non è garantito**: se il primo raid non arriva al 100%, il gioco torna dritto alla schermata di ricompensa senza secondo raid (niente di rotto, solo una run da un raid solo). Se il primo va a buon fine, il gioco passa da solo al secondo in 2-3 secondi (non aspetta nessun timer).
- Il secondo raid a volte offre **truppe bonus aggiuntive** oltre a quelle sopravvissute dal primo (slot 7 e 8 nella barra unità).

## Eseguirlo

Da Telegram (consigliato): menu → "🏗️ Villaggio Secondario" → "▶️ Avvia"/"⛔ Stop"/"📊 Stato".

Da riga di comando sul PC Windows (per test manuali, richiede `cred.bat` caricato):
```
python bot_costruttori.py --cycles 1    # un solo ciclo (doppio raid), per un test supervisionato
python bot_costruttori.py --cycles 0    # loop continuo, finché non lo fermi
```

## Upgrade mura (automatizzato, nel loop)

**Corretta un'ipotesi sbagliata delle sessioni precedenti**: NON è un tap diretto sul segmento di muro con un pannellino a 4-5 pulsanti isolato. È **la stessa tendina/badge "Miglioramenti" del villaggio primario** (tap su `(1115,65)` nella vista di default del Villaggio Costruttori), che si scorre (swipe `(1140,700)->(1140,300)`) cercando voci `Muro`. Su un account rush possono comparire **più voci "Muro" contemporaneamente** a costi diversi (livelli disomogenei) - es. visto dal vivo: "Muro" (8.500 oro), "Muro x130" (127.500 oro), "Muro x41" (204.000 oro) tutte insieme nella stessa tendina.

Selezionando una voce si seleziona **un solo muro fisico specifico**, con un pannello a 4-5 pulsanti: `Info` / (`Selez. Riga`, se compare) / `Migliora ancora` / `Migliora` (oro) / `Migliora` (**"Anello da mura"**, icona ad anello dorato, costo "1" - nome scoperto per caso nella schermata del Pass Stagionale, mai usato in automatico: oggetto raro, mai richiesto dall'utente).

**"Migliora ancora" è il batch, stesso identico meccanismo del villaggio primario**: apre `Togli mura -1` / `Aggiungi mura +10` / `Aggiungi mura +1` / `Migliora` (oro) / `Migliora` (anello) - si aggiungono mura una alla volta con "+1" (mai "+10") controllando il **colore** del costo sul pulsante oro (rosso = fondi insufficienti o mura disponibili esaurite a quel livello), esattamente come `_run_wall_upgrade_round()` nel villaggio primario. Automatizzato in `try_wall_upgrade()`.

**Esiste anche "Selez. Riga"** (seleziona un'intera fila di mura adiacenti, costo totale unico per upgradarle tutte insieme) - esplorato ma **non usato**, su richiesta esplicita dell'utente: si preferisce lo stesso meccanismo +1 del villaggio primario.

**Dialog di conferma finale — diverso da quello del muro singolo, attenzione se si tocca a mano**: dopo "Migliora" (batch) compare "Migliora le mura" / "Vuoi davvero migliorare le mura selezionate per N Oro del costruttore?" con `Annulla`/`OK` — **non** il dialog "Portare al livello N?" con un solo bottone verde che si vede selezionando un muro singolo fuori dal flusso batch. Bug reale trovato il 2026-08-04: usare per errore il punto di conferma del dialog sbagliato fa fallire il pagamento **in silenzio** (nessuna eccezione, nessun errore visibile) - il codice ora verifica esplicitamente che il dialog si sia chiuso (colore di un pixel al centro del box) prima di dichiarare successo.

**Confermato funzionante con soldi veri (2026-08-04)**: 6 mura portate da livello 5 a 6, costo 1.224.000 oro, addebito esatto verificato sul saldo prima/dopo. Un tentativo successivo con oro insufficiente ha correttamente rifiutato di spendere (nessun falso positivo).

**Bug vero trovato e fixato l'8/8/2026 (v0.14): la scelta della voce da migliorare era sbagliata.** Fino alla v0.13 si prendeva "la prima voce Muro trovata scorrendo dall'alto", assumendo fosse la più economica - falso: le voci non sono ordinate per livello nella tendina (verificato dal vivo: livelli 3/4/5/6 sparsi in punti diversi, per niente in quest'ordine). Un primo tentativo di fix (ricordare la posizione di scroll e tornarci per numero di swipe) si è rivelato anch'esso rotto dal vivo: lo scroll non è riproducibile con precisione sufficiente (fino a una riga intera di deriva), causando selezioni sbagliate/comportamento erratico (osservato dall'utente in tempo reale). Fix definitivo:
- Ogni voce "Muro" viene identificata da una **firma testuale scroll-indipendente** (il suffisso "xN", es. "x130") invece che dalla sua posizione sullo schermo - per ritrovarla dopo basta cercarla di nuovo via OCR, non contare gli swipe.
- Il **livello reale** di ogni voce si legge dal titolo che il gioco stesso mostra selezionandola ("Muro (liv. N)", font grande e pulito) invece di indovinarlo dal costo o dalla posizione - `_read_wall_level()`.
- Le voci "Muro" **senza suffisso** (un solo muro rimasto a quel livello, nessun gruppo) vengono lette ma escluse dalla scelta finale: verificato dal vivo che "Migliora ancora" non apre affatto la modalità batch su di esse (il gioco non ha nulla da aggiungere con un solo muro disponibile) - il tentativo di acquisto fallirebbe comunque. Se il livello più basso in assoluto è un muro isolato, resta indietro finché non si aggrega a un gruppo.
- **Bug collaterale trovato durante lo stesso giro di test**: `_wall_cost_is_red()` (il rilevamento "costo in rosso = fondi insufficienti") era mal calibrato per questo villaggio - il crop del colore catturava solo il bordo inferiore del numero di costo invece del numero stesso, causando un falso negativo reale (batch arrivato a 34 mura/4.080.000 oro con solo 3.976.404 disponibili, mai rilevato come insufficiente). Nessuna spesa avvenuta comunque, perché il controllo successivo sul dialog di conferma ha comunque rifiutato di dichiarare successo. Ricalibrato e riverificato su screenshot reali (un caso rosso, uno bianco) prima di rimetterlo in produzione.
- Aggiunto anche un controllo esplicito con `detect_village_type()` prima e dopo la scansione: se il villaggio risultasse cambiato nel frattempo, il giro si interrompe subito con un avviso Telegram invece di continuare alla cieca.
- **Confermato funzionante con soldi veri l'8/8/2026 dopo il fix**: 33 mura portate da livello 4 a 5, costo 3.960.000 oro, saldo sceso esattamente da 3.976.404 a 16.404 (verificato sullo screenshot). Prima del fix definitivo, due tentativi erano falliti in modo sicuro (nessuna spesa, solo giro saltato) proprio grazie ai controlli di sicurezza esistenti.

**Collegato al loop automatico dal 2026-08-04** (v0.13): scatta da solo ogni `WALL_UPGRADE_EVERY` cicli (14 dal v0.29, era 20 - abbassato su richiesta esplicita dell'utente il 2026-08-12), subito dopo un ciclo di attacco riuscito. Un eventuale errore qui non conta come ciclo fallito (l'attacco è comunque andato a buon fine) - loggato e notificato via Telegram a parte, senza contribuire al contatore `consecutive_errors` che fermerebbe la sessione.

## Perché la camera fa solo un pan fisso, non un vero zoom

Tentativi falliti di replicare lo zoom-out che l'utente usa a mano (tasto "freccia giù" tenuto premuto in BlueStacks):
- **Tasto via ADB** (`input keyevent`): nessun effetto — il gioco non ascolta il tasto direttamente, lo zoom è una mappatura di BlueStacks a livello Windows.
- **Tasto sintetico Windows** (`keybd_event` via PowerShell su SSH): nessun effetto — un processo lanciato via SSH gira in una "window station" isolata, non può inviare input alla sessione desktop reale.
- **Pizzico a due dita raw** via `sendevent` sul device touchscreen virtuale: la camera si è mossa ma **non ha zoomato** — solo un pan.

**Soluzione adottata**: uno swipe fisso (non un vero zoom) porta la camera in una posizione ripetibile prima di ogni schieramento. La distanza esatta dello swipe è stata ricalibrata più volte dal vivo (vedi commenti su `CAMERA_SWIPE_START`/`END` nel codice e `recap.md` per la cronologia) — se su una base nuova lo schieramento fallisce sistematicamente, il metodo della griglia (screenshot + overlay a celle, chiedere all'utente dove cade il bordo valido) resta l'opzione più affidabile per ricalibrare, invece di aggiustamenti alla cieca.

## File in questa cartella

- `bot_costruttori.py` — il bot vero, standalone (vedi in cima al file i commenti VERSION/ATTENZIONE per lo stato più aggiornato e le parti ancora da consolidare).
- `prototipo_vecchio_pyautogui.py` — vecchio prototipo pre-ADB, riferimento storico, non riusabile (coordinate su risoluzione diversa, `pyautogui` invece di ADB, nessun controllo di stato/OCR/retry).
